# Git / GitHub

Prefer `gh` CLI over raw `git` for GitHub operations (PR, issue, repo, etc.). Use `gh` whenever a GitHub remote is involved.
Note: `gh` has no `push` subcommand — always use `git push` to push commits.
